# <name> — founder-kit quick-start (html-dashboard)

mode: Default
problem: ★ <what one-page view do I need, from what data?>
output: ★ <single self-contained dashboard.html, opens in browser, no build>
constraints: single file; inline CSS/JS; no external libs/CDN; synthetic data ok; brand if relevant
acceptance: ★ <e.g. shows the key totals + 1 chart; readable; prints clean>
run/check: open dashboard.html in a browser

## Tasks
### Active
- [ ] spec: key trade-offs + edge-cases (keep it brief)
- [ ] build dashboard.html (use the frontend-design skill)
- [ ] /kit-review → fix Blocking
