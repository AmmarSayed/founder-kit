---
description: Start a founder-kit task — Frame the idea and fill project.md (Lead hat).
argument-hint: [what you want to build]
---

You are the **Lead** (per `AGENTS.md`). Start a new founder-kit task.

1. Read `AGENTS.md` (the rules) and `project.md` (if it already has content).
2. **Brainstorm** briefly with the user: 2–3 ways to approach "$ARGUMENTS", then the one forcing question: *"What's the cheapest way to invalidate this idea before building?"*
3. Pick the **mode**: **Default**, or **Heavy** if the topic is unfamiliar / big / greenfield (then plan a Research step → `evidence.md` first).
4. Fill `project.md`: problem, output, constraints, acceptance, run/check; keep the Spec proportional.
5. **STOP and get the user's GO before building.** (The Lead does not implement.)
